{
  "$defs": {
    "SUTDefinition": {
      "description": "A single System Under Test definition.",
      "properties": {
        "type": {
          "description": "e.g., 'llm_api', 'rest_api'. Used to match with test container capabilities.",
          "title": "Type",
          "type": "string"
        },
        "params": {
          "additionalProperties": true,
          "description": "Parameters specific to the SUT type (e.g., base_url, model name, API key).",
          "title": "Params",
          "type": "object"
        }
      },
      "required": [
        "type",
        "params"
      ],
      "title": "SUTDefinition",
      "type": "object"
    }
  },
  "description": "Schema for the top-level SUTs configuration file.",
  "properties": {
    "systems_under_test": {
      "additionalProperties": {
        "$ref": "#/$defs/SUTDefinition"
      },
      "title": "Systems Under Test",
      "type": "object"
    }
  },
  "required": [
    "systems_under_test"
  ],
  "title": "ASQI SUTsConfig",
  "type": "object",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}