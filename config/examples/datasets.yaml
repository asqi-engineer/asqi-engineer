# Example Datasets Configuration
# Defines reusable dataset definitions for data generation

datasets:
  sample_reviews:
    type: "huggingface"
    description: "Small sample dataset of product reviews for demonstration"
    loader_params:
      builder_name: "json"
      data_files: "sample_reviews.json"
    tags:
      - "example"
      - "reviews"
