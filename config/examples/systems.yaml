# Example Systems Configuration (Optional)
# Demonstrates that systems can be provided but are optional for pure data transformation

systems:
  example_llm:
    type: "llm_api"
    params:
      base_url: "http://localhost:4000/v1"
      model: "gpt-4o-mini"
      api_key: "sk-1234"
