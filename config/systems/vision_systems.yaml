# yaml-language-server: $schema=../../src/asqi/schemas/asqi_systems_config.schema.json

systems:
  hf_detr:
    type: "hf_inference_api"
    description: "DETR ResNet-50 via HuggingFace Inference API"
    params:
      model_id: "facebook/detr-resnet-50"
      timeout: 30.0
      api_key: ${HF_TOKEN}
